pipeline {
    agent any

    //environment {
      //  PATH = "$PATH:/home/rahul/.nvm/versions/node/v21.1.0/bin"
    //}
    
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}
